import React from "react";
import PropTypes from "prop-types";

const Item = props => {
  return <div></div>;
};

export default Item;

Item.propTypes = {
  title: PropTypes.string,
  text: PropTypes.string
};
