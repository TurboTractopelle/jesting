
const initialState = []
const articles = (state = initialState, action)=>{
    switch (action.type) {
        case "a":
            return state
    
        default:
            return state
    }

}

export default articles